# Copy this file to .env.local and replace the placeholder with your Google OAuth Client ID
# Create the file at the repository root: e.g. e:\blogbee\.env.local

# Google Identity Services client id (web application)
REACT_APP_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com

# Other example env vars used by the app
MONGODB_URI=mongodb://localhost:27017/blogbee
JWT_SECRET=supersecretjwtkey
REACT_APP_API_URL=http://localhost:5000/api
